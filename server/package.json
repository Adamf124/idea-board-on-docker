{
  "name": "idea-launchpad",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "dev": "concurrently \"nodemon bin/www\" \"cd ./client  && npm start \" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "8.6.0"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^3.6.0",
    "debug": "~2.6.9",
    "dotenv": "^6.0.0",
    "express": "~4.16.0",
    "mongoose": "^5.7.5"
  }
}
